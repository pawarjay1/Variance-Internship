// slice (Extracts a portion of an array without changing the original array.)
-Does NOT change original array
It never changes the original array or string
It always returns a new value
syntax : arr.slice(start,end)

// splice (Adds, removes, or replaces elements by modifying the original array.)
-It change the original array or string 

syntax : array.splice(start, deleteCount, item1, item2, ...)


// eval (eval() executes a string as JavaScript code.)

// replace (it will replace a specific string with defined element)
